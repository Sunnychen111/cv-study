# 视觉/感知算法实习准备与学习计划交接摘要

> 用途：作为后续 ChatGPT 窗口的接手文档，也用于自己复盘学习进度。  
> 当前阶段：**Day 2 已完成，Day 3 准备进入真实 BoxMOT + Hybrid-SORT 接入。**

---

## 1. 个人背景与求职目标

### 教育背景
- 本科：**黑龙江大学，计算机科学与技术**
- 硕士：**广东工业大学，计算机技术**
- 当前状态：研二尚未开学
- 研究方向：**单目标跟踪（Single Object Tracking, SOT）**
- 暂无正式实习经历

### 求职目标
优先考虑广州地区中型公司以上的算法实习，重点方向：
- 计算机视觉算法
- 智能感知
- 自动驾驶 Camera Perception
- Detection / Tracking / MOT
- Robot Perception
- 视觉算法部署
- 可适当扩展 LiDAR / 3D 感知 / BEV

重点关注公司：
- 广汽 / 广汽研究院 / 广汽机器人相关团队
- 小鹏汽车
- 小鹏汇天
- CVTE
- 文远知行
- Pony.ai
- Momenta
- 广州其他中大型机器人、自动驾驶、视觉公司

---

## 2. 当前简历核心经历

### 2.1 MPDTrack：单目标跟踪研究
- 研究问题：Similar-Object Interference（SOI）导致的多峰歧义与身份漂移
- 核心方法：
  - Top-K 候选峰
  - 时序身份记忆
  - reliability-aware reranking
  - Host Response Fallback
- 宿主跟踪器：SUTrack / OSTrack / EVPTrack 等
- 构建 SOIEval 进行相似目标干扰场景专项评估
- 当前有一篇 **AAAI 2027 投稿在审 / Under Review**
- 这段经历主要证明：
  - 算法研究能力
  - 时序建模能力
  - Tracking 专业能力
  - 完整科研实验能力

### 2.2 光电智能吊舱目标检测与跟踪系统
项目主要面向：
- 船只
- 行人
- 无人机

系统核心：
- Detector：**RF-DETR**
- MOT：**Hybrid-SORT**
- Tracker 框架：**BoxMOT**
- 部署设备：**NVIDIA DGX Spark**
- 与已有的光电智能吊舱控制接口进行集成
- 吊舱底层控制不是本人负责，已有手动控制接口，本人负责视觉感知与跟踪链路输出

关键工程经历：
1. 原先使用 YOLO11，但实际场景中对无人机等目标的检测效果不足。
2. 改用 RF-DETR。
3. 当时 BoxMOT 尚未原生提供 RF-DETR 接入方式。
4. 自己实现 RF-DETR 输出到 BoxMOT 跟踪输入之间的 Adapter。
5. 将 RF-DETR + BoxMOT + Hybrid-SORT 整合成检测跟踪 pipeline。
6. 部署至 DGX Spark，并与已有光电吊舱控制链路对接，实现真实船只实时持续跟踪。

推荐简历表达：
> 针对 YOLO11 对无人机等目标检测效果不足的问题，引入 RF-DETR，并在 BoxMOT 尚未提供对应接入方案时，独立完成 RF-DETR detector output 到 BoxMOT/Hybrid-SORT tracking pipeline 的接口适配与系统集成。

### 2.3 桌面机器人轻量级跌倒检测
- 工业相机作为视频输入
- 使用 **MediaPipe Pose**
- 根据人体骨架关键点设计几何特征和手工阈值规则进行跌倒判定
- 部署平台：**NVIDIA Jetson Nano**
- 连续视频稳定约 **25 FPS**
- 项目定位：
  - 轻量化 Robot Vision
  - 边缘设备部署
  - 实时视觉 pipeline
  - 工程取舍与实时性

---

# 3. 技术栈补齐总体目标

当前主要通过 AI Coding 辅助编码，因此学习目标不是停止使用 AI，而是做到：

> AI 可以提升效率，但拿掉 AI 后，仍然可以读懂、修改、调试、解释视觉算法工程。

重点补齐：

### P0
- Python
- NumPy
- PyTorch
- OpenCV
- Tensor / shape / dtype / device
- Linux
- Git

### P1
- C++ 基础
- Detection 基础
- MOT 基础
- ONNX
- TensorRT
- 自动驾驶 Camera / 3D / BEV 基础

### P2
- ROS2
- TCP / UDP / 串口通信
- Docker

暂时不优先深入：
- CUDA kernel 编程
- 大规模刷 LeetCode
- 再复现大量 Tracking 论文

---

# 4. Day 1 已完成内容

## Day 1 目标
解决视觉代码中最基础的数据结构与 Tensor 操作问题。

### 4.1 IoU 与 BBox
已完成：
- 手写 IoU
- 理解：
  - intersection
  - union
  - `max(0, ...)`
- 支持：
  - 部分重叠
  - 不相交
  - 完全包含
  - 完全相同
- 理解非法 bbox：
  - 负坐标不一定非法
  - `x2 < x1` / `y2 < y1` 才属于定义异常

已实现：
```python
xywh -> xyxy
xyxy -> xywh
```

关键结论：
```text
xywh = [x, y, w, h]
xyxy = [x1, y1, x2, y2]

xywh -> xyxy:
[x, y, x+w, y+h]

xyxy -> xywh:
[x1, y1, x2-x1, y2-y1]
```

不能用 `abs()` 随意修复 bbox。

---

## 4.2 HWC / CHW / BCHW
已掌握：

```text
OpenCV / NumPy:
[H, W, C]

PyTorch:
[C, H, W]

模型输入:
[B, C, H, W]
```

实际完成：

```python
image_sw = np.transpose(image, (2, 0, 1))
tensor = torch.from_numpy(image_sw)
x = tensor.unsqueeze(0)
```

理解：
```text
unsqueeze = 增加 size=1 的维度
squeeze   = 删除 size=1 的维度
```

注意：
```python
x.squeeze()
```
会删除所有大小为 1 的维度。

---

## 4.3 reshape / permute / transpose
已理解：

### reshape
- 改变数据的形状解释
- 元素总数不变
- 不代表维度语义交换

### permute
- 重排 Tensor 维度
- 例如：
```python
HWC -> CHW
x.permute(2, 0, 1)
```

### PyTorch transpose
- 一次交换两个维度

### NumPy transpose
- 可通过 axes 一次重排多个维度

重要结论：
> HWC → CHW 应使用 permute / transpose，而不能用 reshape。

同时明确：
- reshape ≠ resize
- reshape ≠ crop

---

## 4.4 图像 dtype 与 normalization
已完成：

```text
uint8 0~255
↓
float32
↓
/255
↓
float32 0~1
```

代码：

```python
x = x.float() / 255.0
```

理解：
- float32 不是单纯因为 sigmoid
- 神经网络主要进行浮点计算
- 权重通常为 float32 / float16
- normalization 与 dtype conversion 是不同概念
- `float32` 并不意味着已经归一化

---

## 4.5 RGB / BGR
理解：
- OpenCV 默认 BGR
- 很多深度学习模型训练使用 RGB

转换：

```python
cv2.cvtColor(image, cv2.COLOR_BGR2RGB)
```

训练和推理颜色通道不一致会造成输入分布偏移和性能下降。

---

## 4.6 Tensor Index / Slice
已掌握：

给：
```python
x.shape = [B, C, H, W]
```

例如：

```python
x[0]
x[0, 0]
x[:, 0]
x[:, :, :100, :100]
x[2, :, 100:300, 200:500]
```

理解：
- 使用单个 index 会消掉该维度
- `x[0]` 与 `x[0:1]` shape 不同

---

## 4.7 Boolean Mask
已掌握：

```python
scores = torch.tensor([...])
mask = scores >= 0.5
filtered_scores = scores[mask]
```

多条件：

```python
mask = (scores >= 0.5) & (class_ids != 2)
```

或：

```python
mask = (class_ids == 0) | (class_ids == 1)
```

注意 Tensor 条件中通常使用：
```text
&  |
```
而不是 Python 的：
```text
and or
```

---

## 4.8 Detection Tensor
已练习：

```text
[N, 6]
[x1, y1, x2, y2, confidence, class_id]
```

提取：

```python
boxes = detections[:, :4]
scores = detections[:, 4]
class_ids = detections[:, 5]
```

并完成 confidence / class filtering。

---

# 5. Day 2 已完成内容

## Day 2 目标
完成最小 PyTorch inference pipeline，掌握 CPU/GPU 与常见 Debug，并重构 Detector → Tracker Adapter。

---

## 5.1 SimpleModel 推理
已实现一个简单 PyTorch 网络：

```text
[B,3,H,W]
↓ Conv2d
[B,8,H,W]
↓ AdaptiveAvgPool
[B,8,1,1]
↓ flatten(1)
[B,8]
↓ Linear
[B,3]
```

推荐：

```python
x = x.flatten(1)
```

而不是不加维度指定的 `squeeze()`。

---

## 5.2 model.eval() 与 inference_mode()
已理解：

### model.eval()
作用：
- 切换模型到 evaluation mode
- 主要影响：
  - Dropout
  - BatchNorm

### torch.inference_mode()
作用：
- 关闭 autograd / 梯度跟踪
- 减少推理阶段计算和内存开销

标准推理：

```python
model.eval()

with torch.inference_mode():
    output = model(x)
```

二者不是同一个功能。

---

## 5.3 CPU / GPU Device
已掌握：

```python
device = torch.device(
    "cuda" if torch.cuda.is_available() else "cpu"
)

model = model.to(device)
x = x.to(device)
```

检查：

```python
x.device
next(model.parameters()).device
```

---

## 5.4 Debug 实战
实际主动制造并解决过以下错误：

### Device mismatch
```text
CPU Tensor
vs
CUDA Model
```

报错类似：
```text
Input type (torch.FloatTensor)
weight type (torch.cuda.FloatTensor)
```

### Channel mismatch
模型：
```text
Conv2d in_channels = 3
```

输入：
```text
[B,1,H,W]
```

报错：
```text
expected input to have 3 channels, but got 1
```

### dtype mismatch
输入：
```text
torch.uint8
```

模型：
```text
torch.float32
```

报错：
```text
Input type (unsigned char)
and bias type (float)
should be the same
```

当前 Debug 固定检查顺序：

```python
print(x.shape)
print(x.dtype)
print(x.device)

p = next(model.parameters())

print(p.dtype)
print(p.device)
```

并学习 traceback 阅读方式：
1. 先看最后一行错误
2. 判断错误类别
3. 向上找到第一次进入自己代码的位置
4. 检查对应变量

---

## 5.5 综合 PyTorch Inference Pipeline
已独立完成：

```text
NumPy uint8 image
[H,W,C]
↓
torch.from_numpy
↓
permute(2,0,1)
↓
float()/255
↓
unsqueeze(0)
↓
to(device)
↓
model.eval()
↓
torch.inference_mode()
↓
output
```

实际输出正确：

```text
input shape: [1,3,480,640]
input dtype: float32
input device: cuda:0

model device: cuda:0

output shape: [1,3]
output dtype: float32
output device: cuda:0
```

---

# 6. Day 2：Detector Adapter 重构

## 6.1 BoxMOT 统一输入协议
当前学习中统一定义：

```text
[N,6]
[x1,y1,x2,y2,conf,class_id]
```

已实现：

```python
def build_boxmot_input(boxes, scores, class_ids):
    return np.column_stack(
        (boxes, scores, class_ids)
    ).astype(np.float32)
```

已经验证：
- 正常输入 → `(N,6), float32`
- 空输入 → `(0,6), float32`

---

## 6.2 RF-DETR Adapter
当前重构：

```python
def rfdetr_to_boxmot(result):
    boxes = result.xyxy
    scores = result.confidence
    class_ids = result.class_id

    return build_boxmot_input(
        boxes,
        scores,
        class_ids
    )
```

理解：
RF-DETR / supervision 风格：
```text
.xyxy
.confidence
.class_id
```

---

## 6.3 YOLO Adapter
已完成：

```python
def yolo_to_boxmot(result):
    boxes = result.boxes.xyxy
    scores = result.boxes.conf
    class_ids = result.boxes.cls

    boxes = boxes.detach().cpu().numpy()
    scores = scores.detach().cpu().numpy()
    class_ids = class_ids.detach().cpu().numpy()

    return build_boxmot_input(
        boxes,
        scores,
        class_ids
    )
```

YOLO / Ultralytics 风格：

```text
result.boxes.xyxy
result.boxes.conf
result.boxes.cls
```

理解 Adapter 的意义：

```text
RF-DETR ──┐
          ↓
    model-specific adapter
          ↓
       [N,6]
          ↓
       BoxMOT

YOLO ─────┘
```

Adapter 负责：
- 字段名称统一
- 数据格式统一
- dtype 统一
- Detector 与 Tracker 解耦

---

# 7. Day 2：Fake Tracker Demo 已完成

为了先理解 Tracker 接口，实现：

```python
fake_tracker_update(dets)
```

输入：

```text
[N,6]
[x1,y1,x2,y2,conf,cls]
```

模拟输出：

```text
[N,8]
[x1,y1,x2,y2,track_id,conf,cls,det_index]
```

已掌握：

```python
boxes = tracks[:, :4]
track_ids = tracks[:, 4]
scores = tracks[:, 5]
class_ids = tracks[:, 6]
```

过滤：

```python
mask = (scores >= 0.8) & (class_ids == 0)
boat_tracks = tracks[mask]
```

目标 ID 提取：

```python
target_mask = track_ids == target_id
target_tracks = tracks[target_mask]
```

BBox 中心：

```python
cx = (x1 + x2) / 2
cy = (y1 + y2) / 2
```

---

# 8. Day 2：OpenCV Tracking Demo 已完成

创建：

```python
frame = np.zeros(
    (720, 1280, 3),
    dtype=np.uint8
)
```

解析 track：

```python
x1 = int(track[0])
y1 = int(track[1])
x2 = int(track[2])
y2 = int(track[3])

track_id = int(track[4])
score = float(track[5])
class_id = int(track[6])
```

画框：

```python
cv2.rectangle(...)
cv2.putText(...)
```

已经成功显示两个模拟目标：

```text
ID:1 CLS:0 0.91
ID:2 CLS:2 0.82
```

Debug 经验：
- `cv2.rectangle()` bbox 坐标需要转成整数
- 不应把整个 `tracks` 转 int，否则 confidence 如 0.91 会丢失
- 只在 Visualization 阶段转换 bbox / ID / class
- 曾出现只画一个框的问题，原因是 `cv2.rectangle()` 未缩进到 `for track in tracks:` 内

---

# 9. 当前进度节点

到目前为止已经完整理解：

```text
Detector Output
      ↓
Detector-specific Adapter
      ↓
Standard Detection
[N,6]
      ↓
Fake Tracker
      ↓
Tracks
[N,8]
      ↓
Track Parsing
      ↓
OpenCV Visualization
```

也就是说：

> **Fake Detection → Adapter → Fake Tracker → Visualization 已完全跑通。**

---

# 10. Day 3 计划：真实 BoxMOT + Hybrid-SORT

## Day 3 总目标
把：

```python
tracks = fake_tracker_update(dets)
```

替换为：

```python
tracks = tracker.update(dets, frame)
```

完成真正的 MOT pipeline。

Day 3 不再学习控制系统，吊舱底层控制不属于当前负责范围。

---

## Day 3 第一阶段：BoxMOT / Hybrid-SORT 环境
目标：
- 安装 BoxMOT
- 确认当前版本
- 确认 Hybrid-SORT 创建方式
- 确认 ReID 权重要求
- 创建 tracker

重点理解：

```text
BoxMOT
→ Tracker Factory / Registry
→ Hybrid-SORT
```

---

## Day 3 第二阶段：Fake Detection + Real Hybrid-SORT
保持目前假检测结果：

```text
boxes
scores
class_ids
↓
build_boxmot_input()
↓
[N,6]
```

只替换：

```text
fake_tracker_update()
```

为：

```text
tracker.update(dets, frame)
```

目标：
- 能成功调用真实 Hybrid-SORT
- 理解输出 shape
- 检查 track_id 是否生成
- 继续复用当前 OpenCV Visualization

---

## Day 3 第三阶段：视频多帧跟踪
从：

```text
单张黑色测试图
```

扩展成：

```text
cv2.VideoCapture
↓
while loop
↓
frame-by-frame detection
↓
Hybrid-SORT update
↓
Track ID
↓
Visualization
```

重点理解：
- 为什么 MOT 必须逐帧更新
- track state 如何跨帧保存
- 为什么 Fake Tracker 不能真正进行 Tracking
- Track ID 如何在多帧中维持

---

## Day 3 第四阶段：YOLO + BoxMOT
先使用 YOLO，因为当前 API 与生态更加成熟：

```text
Video
↓
YOLO
↓
yolo_to_boxmot()
↓
[N,6]
↓
Hybrid-SORT
↓
Tracks
↓
OpenCV
```

目标：
- 完成第一个真正的 Detection + MOT Demo

---

## Day 3 第五阶段：RF-DETR + BoxMOT
完成：

```text
Video
↓
RF-DETR
↓
rfdetr_to_boxmot()
↓
[N,6]
↓
Hybrid-SORT
↓
Tracks
↓
Visualization
```

此时就基本重建当年 DGX Spark 项目的视觉主干。

---

# 11. Day 4 后续建议

Day 4 建议进入 **MOT 原理**，不要只停留在调用 BoxMOT。

重点：

### Kalman Filter
理解：
- state
- predict
- update
- motion model

### Hungarian Algorithm
理解：
- detection ↔ track assignment
- cost matrix
- one-to-one matching

### IoU Association
理解：

```text
Previous Tracks
       +
Current Detections
       ↓
IoU Matrix
       ↓
Matching
```

### ReID
理解：
- appearance feature
- cosine similarity
- 为什么遮挡后可重新关联
- Hybrid-SORT 为什么需要多种 cue

最终能够回答：

> Detector 和 Tracker 各自负责什么？

> 为什么 Detection 好不代表 Tracking 一定好？

> 为什么 detector 漏检会导致轨迹中断？

> SOT 和 MOT 的主要区别是什么？

---

# 12. Day 5 后续建议

进入部署工程：

```text
PyTorch
↓
ONNX
↓
ONNX Runtime
↓
TensorRT
↓
FP16 inference
```

重点：
- ONNX export
- dynamic shape
- input/output name
- TensorRT engine
- FP32 / FP16 / INT8
- latency / FPS / throughput
- warm-up

与已有项目对应：
- Jetson Nano
- DGX Spark
- 实时视觉 inference

---

# 13. 后续自动驾驶技术栈

在基础工程完成后再学习：

```text
2D Detection
↓
MOT
↓
LiDAR / Point Cloud
↓
3D Detection
↓
BEV
↓
Occupancy
```

建议认识：
- KITTI
- nuScenes
- Waymo
- PointPillars
- CenterPoint
- BEVFormer
- BEVFusion

当前目标不是复现所有模型，而是达到：
> 能看懂广汽 / 小鹏自动驾驶视觉与感知实习 JD，并能完成基础技术面试。

---

# 14. 当前最重要的学习原则

1. **AI Coding 可以继续使用，但先自己判断 shape / dtype / device / 数据流。**
2. 出现报错先看 traceback 最后一行。
3. 优先找到自己代码中的第一处错误位置。
4. 模型推理异常优先检查：
   ```text
   shape
   dtype
   device
   range
   ```
5. 不只记框架 API，要理解：
   ```text
   Input → Processing → Output
   ```
6. 项目中哪些是自己做的、哪些是开源框架、哪些是已有系统，需要明确边界。
7. 不夸大吊舱控制工作：
   - 本人负责视觉检测与跟踪 pipeline
   - 吊舱控制已有接口
   - 视觉结果接入现有控制系统

---

# 15. 下一窗口接手提示

如果在新的 ChatGPT 窗口继续，请直接说明：

> 我正在进行视觉/感知算法实习技能补齐。Day 1 已完成 IoU、BBox、PyTorch Tensor、HWC/CHW/BCHW、reshape/permute、dtype、Boolean Mask、Detection Tensor；Day 2 已完成 PyTorch inference、CPU/GPU、Debug、RF-DETR/YOLO → BoxMOT Adapter、Fake Tracker 和 OpenCV Tracking Demo。现在开始 Day 3，需要接入真实 BoxMOT + Hybrid-SORT，把 fake_tracker_update(dets) 替换成 tracker.update(dets, frame)，然后完成 YOLO + Hybrid-SORT 和 RF-DETR + Hybrid-SORT 的视频 Demo。

---

## 当前状态一句话总结

**Day 1：视觉/PyTorch 数据基础完成。**  
**Day 2：Inference + Debug + Detector Adapter + Fake MOT Demo 完成。**  
**Day 3：开始真实 BoxMOT + Hybrid-SORT，多帧 MOT Demo。**
